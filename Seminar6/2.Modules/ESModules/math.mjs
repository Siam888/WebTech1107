// math.js (ESM)
// export a tiny function
export function add(a, b) {
    return a + b;
}

// export {add};

// ECMAScript is the official language spec: it defines the syntax and semantics (what let, class, Promise, async/await, Map/Set, Array.prototype.map, modules, Proxy, Intl, etc. mean) and the built-in standard library.
// ESModules work in browsers by default, and in Node.js when ESM is enabled (e.g., using .mjs or "type": "module").